# Frontend-specific environment variables

# API URL - When not set, the frontend will dynamically calculate the API URL
# based on the current port the frontend is running on
# VITE_API_URL=http://localhost:3001/api

# Other environment variables
VITE_APP_NAME=LullAIbot Education Platform
VITE_APP_VERSION=1.0.0 